#ifndef FILE_MYWORK_SEEN
#define FILE_MYWORK_SEEN

/*
namespace util {
  class Rect;
  class Point;
  class Region;
}

//sort of useless actually, since cv provides rect with is same as my region
namespace util {
  class Rect {
  public:
    Rect();
    Rect(int width, int height) { w = width; h = height; }
    int width() { return w; }
    int height() { return h; }
  private:
    int w = 0;
    int h = 0;
  };

  class Point {
  public:
    Point();
    Point(int x, int y) { px = x; py = y; }
    int x() { return px; }
    int y() { return py; }
  private:
    int px = 0;
    int py = 0;
  };

  class Region {
  public:
    Region(Rect rect, Point point) { r = rect; p = point; }
    Rect rect() { return r; }
    Point point() { return p; }      
  private:
    Rect r;
    Point p;
  };
  
}
*/

#endif
